# TODO

## mob: ball_sensor の ADC 読み取りエラー(散発)

SEN コマンド要求時に稀に以下のエラーが出る(2026-07-24 に2回観測)。

```
E (171881) adc_oneshot: adc_oneshot_get_calibrated_result(330): adc oneshot read fail
```

- ボールセンサ(IO14、ADC2)の読み取りで一過性に失敗している模様
- SEN 応答自体は返る(ball_raw=0 になる)ので実害は今のところ小さい
- 2回ともアイドル状態からの単発 SEN 要求時に発生
- 再発頻度が上がるようなら ball_sensor.cpp の ADC 設定
  (ADC2 と battery/壁センサとの共存、キャリブレーション設定)を調査する

## micromouse関連TODO(保留中、2026-08-02にコード削除)

以下2件は `software/micromouse/` のコード自体を2026-08-02に削除した
(CLAUDE.md参照、ESP32制御ロジックの根本的な作り直しのため)ため、
直接は着手できない。再実装時の参考として内容は残す。

## micromouse: カメラ角度補正(vision.py)の較正精度

2026-07-24、software/micromouse/vision.py の赤帯上端エッジ検出で
9枚の較正画像(±10度・±25mm)全てが残差1px未満でフィットできる
ところまで実装したが、傾き↔ヨー角の関係を詰め切れていない。

- `slope_deg ≈ 0.53×yaw_deg + 3.12`(R²=0.95、9点のみの経験的フィット)。
  切片3.12度(カメラ取付誤差か基準姿勢自体のズレか未確認)、ゲイン0.53
  (幾何学的な裏付けなし)の両方とも要検証。
- 残差に横位置ズレ(lateral_mm)との相関がありそうで、単一の壁の傾き
  だけではヨー角と横位置ズレの影響を分離できていない可能性がある
  (2本の壁の消失点を使う当初案なら分離できるはずだった)。
- サンプルが3×3=9点のみで、線形性・距離依存性の検証には不十分。
- 赤色しきい値(RED_R_MIN等)・クラスタgapしきい値(CLUSTER_GAP_PX=30)は
  1〜2枚の実画像から決めた経験値で、照明・壁配置の多様性に対する
  頑健性は未検証。

mob側の SANG コマンド実装・実際の補正ロジックが動くようになった後、
上記の較正精度を詰める。

### 較正範囲外での破綻(2026-07-24 実機で確認)

`hw_test.py camera-straighten`(角度→距離の順で複数回自動補正するコマンド、
JOGTURN/JOGFWD/JOGBACKで低速に動かす)は、較正時の基準姿勢付近では
3回のループで残差0.2度・0.3mm程度まで安定して収束することを確認した。

一方、較正範囲(±10度)を大きく超えた状態(この時は直前の壊れた推定
[resid=145px相当]による誤補正で機体が大きくズレていた)から始めると、
残差自体は毎回1px前後とクリーンに見えるにもかかわらず、0度ではなく
+17度付近の誤った平衡点に収束してしまう現象を確認した。JOGTURN等の
符号・補正ロジック自体は基準姿勢付近の再テストで正しいことを確認済み
なので、原因は較正時と別の壁(または別の距離・角度)を追跡してしまい、
その壁ではGAIN/INTERCEPTが合わなくなっている可能性が高い
(「一番手前のクラスタ」を選ぶ現在の方式は、視界内で追跡対象の壁が
すり替わることを検知できない)。

対策候補: 残差だけでなく、フィット後の推定値が較正時のレンジ
(±10度・手前クラスタのrow範囲など)から外れていないかをチェックし、
外れていたら補正を保留する信頼度ゲートを入れる。

→ 対応済み(2026-07-24): `software/micromouse/camera_correction.py` に
`is_confident()`(残差2px・ヨー角15度・距離オフセット40mmの上限)を実装し、
探索走行本体(state_machine.py)・hw_test.py camera-correct/camera-straighten
の両方で使うようにした。ただし上限値自体は経験的な安全マージンであり、
較正データを増やして根拠を強化する余地は残っている。

## micromouse: カメラ補正はX軸・Y軸を同時に補正できない

2026-07-24、探索走行(state_machine.py)へのカメラ補正組み込みで、カメラが
前方固定のため「現在向いている軸」の前壁でしか補正できない設計にした
(ユーザー確認済み)。機体は探索中に東西南北すべてを向くため、セッション
全体で見ればX軸・Y軸どちらも別々のタイミングで補正されるが、1回の補正で
同時に両軸を直すことはできない。側壁(進行方向に垂直な壁)を同時検出して
垂直方向も同時に補正するには、新しい検出ロジック(消失点法など、当初検討
していた方式)と側壁用の実機較正データ収集が別途必要。
