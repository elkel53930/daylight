# Daylight マイクロマウス機能実装 指示書

## 1. 目的

このタスクでは、`daylight` リポジトリにマイクロマウス競技のための自律走行機能を実装する。

開発対象は以下のリポジトリである。

* 実装対象: `daylight`
* 参考実装: [`robosweep_twilight`](https://github.com/elkel53930/robosweep_twilight)

`Daylight` は `Twilight` の後継機である。

したがって、`robosweep_twilight` に存在する既存の走行制御、センサ処理、モータ制御、状態機械、デバッグ機能などを十分に調査し、再利用可能な設計・アルゴリズム・実装上の知見を活用する。

ただし、TwilightとDaylightではハードウェアおよびソフトウェア構成が異なる可能性があるため、コードを機械的にコピーしてはならない。

最終的な実装は、必ず `daylight` の現在のアーキテクチャ、API、ハードウェア構成、コーディング規約に適合させること。

---

# 2. 基本方針

## 2.1 まず既存コードを調査する

実装を開始する前に、以下を必ず調査すること。

### Daylight

`daylight` リポジトリについて、以下を把握する。

* ディレクトリ構成
* `elec` の構成
* `software` の構成
* MCUおよびRaspberry Pi CM4/CM5などの役割分担
* モータ制御の実装
* エンコーダ入力
* 壁センサ入力
* IMU/ジャイロ入力
* 通信インターフェース
* 走行制御周期
* RTOSやタスク構成
* Rust/C++/Pythonの役割
* 既存の手動操作機能
* 既存の状態機械
* 既存のテストコード

### Twilight

`robosweep_twilight` リポジトリについて、特に以下を調査する。

* 走行制御
* モータ速度制御
* エンコーダ処理
* ジャイロ処理
* 壁センサ処理
* センサキャリブレーション
* 直進制御
* 旋回制御
* 迷路表現
* 迷路探索
* 走行状態機械
* 手動走行と自律走行の切り替え
* 安全停止処理
* ログ出力
* テスト方法

Twilightのコードを読む際は、単に「何をしているか」だけでなく、

> なぜその処理が必要なのか

を理解すること。

特に、センサの遅延、モータの応答、走行体の機械的特性、壁との距離、旋回時の挙動など、実機依存の処理を明確に区別すること。

---

# 3. 競技上の前提

実装対象は、一般的な全日本マイクロマウス競技で使用される16×16迷路を基本とする。

基本的な前提:

* 迷路サイズ: 16×16セル
* 1セル: 180 mm
* ゴール: 迷路中央の2×2セル
* スタート地点: 迷路外周上
* 探索時は未知の壁情報を取得しながら走行する
* ゴール到達後、既知の迷路情報から最短経路を計算する
* 最短経路を再走行する

ただし、競技規則の細部は実装時点の公式規則を確認すること。

競技規則をハードコードした独立のロジックにするのではなく、可能な限り、

* 迷路サイズ
* セルサイズ
* ゴール位置
* 探索時間
* 走行モード

を設定可能な構造にする。

---

# 4. 実装する機能

最低限、以下の機能を実装する。

## 4.1 マイクロマウスモード

Daylightに以下の自律走行モードを追加する。

```text
IDLE
  ↓
MICROMOUSE_START
  ↓
EXPLORATION
  ↓
GOAL_REACHED
  ↓
PATH_PLANNING
  ↓
SPEED_RUN
  ↓
FINISHED
```

必要に応じて以下の状態を追加する。

```text
ERROR
EMERGENCY_STOP
RETURN_TO_START
CALIBRATION
```

状態遷移は明確に定義すること。

特に、

* 何をもって探索開始とするか
* 何をもってゴール到達と判定するか
* ゴール到達後にいつ停止するか
* 最短経路をいつ計算するか
* 最短経路走行をいつ開始するか
* 異常時にどのように停止するか

を曖昧にしないこと。

---

# 5. 迷路データ構造

迷路は16×16のセルを基本とする。

各セルには少なくとも以下の情報を保持する。

```text
Cell
├── north wall
├── east wall
├── south wall
├── west wall
└── visited
```

壁情報は最低でも以下の3状態を区別できることが望ましい。

```text
UNKNOWN
OPEN
WALL
```

探索開始時は、既知の外周壁を初期化する。

内部の壁は未知状態とする。

例:

```text
UNKNOWN
UNKNOWN
WALL
OPEN
```

のように、未探索の壁と探索済みの壁を区別できるようにする。

## 5.1 壁情報の整合性

迷路の壁は、隣接セル間で整合していなければならない。

例えば、

```text
cell(x, y).east == WALL
```

なら、

```text
cell(x + 1, y).west == WALL
```

でなければならない。

壁情報を更新する処理は、一方のセルだけを更新するのではなく、隣接セルも同時に更新すること。

---

# 6. 探索アルゴリズム

探索アルゴリズムは、既存のマイクロマウス実装およびTwilightの設計思想を参考にする。

基本的には、現在位置からゴールまでの距離を迷路情報から計算し、走行可能な方向の中から次のセルを選択する。

実装候補:

* 足立法
* Flood Fill
* BFSベースの距離マップ
* 未知壁を考慮した探索戦略

今回の実装では、迷路探索と最短経路計画を分離すること。

推奨構成:

```text
Maze
 ├── wall map
 ├── visited map
 └── distance map

Explorer
 ├── update sensor information
 ├── update maze
 ├── calculate distance
 └── select next cell

PathPlanner
 ├── calculate shortest path
 └── generate motion sequence
```

探索アルゴリズムを走行制御コードに直接埋め込まないこと。

---

# 7. 探索時の走行

探索時は、各セルに到達するたびに以下を実行する。

```text
1. 現在位置を更新
2. 壁センサから壁情報を取得
3. 迷路データを更新
4. 訪問済み状態を更新
5. ゴール到達判定
6. 次の目標セルを決定
7. 次の方向を決定
8. 必要なら旋回
9. 次のセルまで走行
```

重要なのは、

> 走行制御の結果として迷路情報を更新する

のではなく、

> センサによって観測した壁情報をもとに迷路を更新し、その結果から次の行動を決定する

という構造にすることである。

---

# 8. ゴール判定

ゴールは中央の2×2セルである。

ゴール到達判定は、以下のいずれかの方法で実装する。

* 現在の迷路座標がゴール領域に入った
* ゴール領域の中央に到達した
* 競技上必要な停止条件を満たした

ゴール領域をハードコードした複数の条件に分散させないこと。

以下のような単一の定義を持つこと。

```text
GoalRegion
├── x_min
├── x_max
├── y_min
└── y_max
```

---

# 9. 最短経路計画

ゴール到達後は、既知の迷路情報からスタート地点または指定された出発地点からゴールまでの最短経路を計算する。

経路は、単なるセル列ではなく、可能であれば走行命令列に変換する。

例:

```text
Cell path:

F → F → R → F → L → F

↓ convert

Motion sequence:

STRAIGHT(2 cells)
TURN_RIGHT
STRAIGHT(1 cell)
TURN_LEFT
STRAIGHT(1 cell)
```

将来的な高速走行を考慮し、以下を分離すること。

```text
Path planning
    ↓
Cell path
    ↓
Motion planning
    ↓
Trajectory
    ↓
Low-level control
```

---

# 10. 走行制御

Daylightの既存の走行制御を最優先で利用する。

Twilightの走行制御を参考にする場合は、以下を明確に分離する。

## 10.1 低レベル制御

```text
Motor control
Encoder control
Velocity control
Current control
```

## 10.2 姿勢制御

```text
Gyro
Yaw control
Angular velocity control
```

## 10.3 壁制御

```text
Wall sensor
Wall distance control
Centering control
```

## 10.4 軌道制御

```text
Straight motion
Turn motion
Acceleration
Deceleration
```

## 10.5 マイクロマウス制御

```text
Cell transition
Maze position
Direction
Next action
```

これらを一つの巨大な関数にまとめないこと。

---

# 11. センサ処理

壁センサの値は、そのまま壁の有無として扱わないこと。

必要に応じて、

```text
raw sensor value
    ↓
filter
    ↓
calibration
    ↓
wall detection
    ↓
wall state
```

という処理段階を設ける。

最低限、以下を考慮する。

* センサ値のノイズ
* センサ個体差
* 壁までの距離
* 走行速度による影響
* 旋回中のセンサ値
* 壁が存在しない場合
* 壁が近すぎる場合
* センサ異常

Twilightに既存のキャリブレーション処理がある場合は、必ず調査してDaylightの方式と比較する。

---

# 12. 走行制御の座標系

マイクロマウスでは、以下の2種類の座標系を混同しないこと。

## 12.1 連続座標系

走行制御用。

```text
x [m]
y [m]
theta [rad]
velocity [m/s]
angular velocity [rad/s]
```

## 12.2 迷路座標系

探索用。

```text
cell_x
cell_y
direction
```

両者の関係は明確に管理する。

例:

```text
Continuous Pose
    ↓
Cell Position Estimator
    ↓
Maze Coordinate
```

または、

```text
Maze Motion Command
    ↓
Trajectory Generator
    ↓
Continuous Motion Controller
```

のように変換層を設ける。

---

# 13. Twilightからの移植方針

Twilightのコードを移植する際は、以下の順序で作業する。

## Step 1: Twilightの機能マップを作成

以下の表を作成する。

```text
Twilight機能
    ↓
実装ファイル
    ↓
依存するハードウェア
    ↓
Daylight対応状況
    ↓
移植方針
```

例:

```text
壁センサ処理
  ↓
twilight/software/...
  ↓
センサ基板A
  ↓
Daylightではセンサ基板B
  ↓
アルゴリズムのみ再利用
```

## Step 2: Daylightの既存機能を優先

Daylightに既に同等の機能がある場合は、Twilightの実装を移植せず、Daylightの実装を利用する。

## Step 3: 共通アルゴリズムのみ再利用

以下は比較的再利用しやすい。

* 迷路データ構造
* BFS
* Flood Fill
* 経路生成
* 状態機械の考え方
* センサ値の判定ロジック

一方、以下はハードウェア依存である可能性が高い。

* GPIO番号
* ADCチャンネル
* PWM設定
* モータドライバAPI
* エンコーダAPI
* I2C/SPIデバイス
* センサの閾値
* タイミング
* 割り込み処理

これらはそのままコピーしないこと。

---

# 14. 実装順序

以下の順序で実装する。

## Phase 1: 調査

* Daylightの構成を調査
* Twilightの構成を調査
* 既存機能の比較
* ハードウェア差分の整理
* 実装方針を文書化

この段階では、いきなり大量のコードを書かないこと。

---

## Phase 2: 迷路ソフトウェア

ハードウェアに依存しない以下の機能を実装する。

* Maze
* Cell
* Wall
* Position
* Direction
* GoalRegion
* DistanceMap
* Explorer
* PathPlanner

可能な限りユニットテストを追加する。

最低限、以下をテストする。

* 壁の追加
* 隣接セルの壁整合性
* 迷路境界
* ゴール判定
* BFS
* Flood Fill
* 最短経路
* 方向変換
* 180度旋回
* 行き止まり

---

## Phase 3: センサ統合

実際の壁センサから以下を取得する。

```text
front wall
left wall
right wall
rear wall
```

ただし、後方センサが存在しない場合は無理に要求しない。

センサ観測結果を迷路座標系の壁情報に変換する。

例:

```text
Robot Direction = NORTH

left sensor
    ↓
WEST wall

front sensor
    ↓
NORTH wall

right sensor
    ↓
EAST wall
```

ロボットの向きによる座標変換を独立した処理として実装する。

---

## Phase 4: 1セル走行

まず迷路探索を実装する前に、

```text
1セル直進
```

を安定して実行できることを確認する。

次に、

```text
90度左旋回
90度右旋回
180度旋回
```

を実装する。

セル単位の走行が安定しない状態で探索アルゴリズムを統合しないこと。

---

## Phase 5: 迷路探索

以下の流れを実機で実現する。

```text
セル走行
    ↓
センサ観測
    ↓
迷路更新
    ↓
次セル決定
    ↓
旋回
    ↓
セル走行
```

最初は低速で実装する。

高速化は、基本動作の安定後に行う。

---

## Phase 6: ゴール到達

中央の2×2ゴール領域への到達を確認する。

ゴール到達後は、

```text
停止
```

または、

```text
最短経路計算
```

へ遷移する。

この挙動は状態機械として明確に実装する。

---

## Phase 7: 最短経路走行

探索によって得られた迷路情報を用いて、

```text
最短経路計算
    ↓
経路圧縮
    ↓
走行命令生成
    ↓
走行
```

を実装する。

---

# 15. 最初の実装では高速化しない

最初の目標は、

> 迷路を正しく認識し、正しく走行し、ゴールに到達すること

である。

以下は初期実装では優先しない。

* 最高速度
* 最小走行時間
* 複雑なスラローム
* 高度な軌道最適化
* 最適な加速度
* 最短時間走行

まずは、

```text
正しく止まる
正しく曲がる
正しく壁を認識する
正しく迷路を更新する
正しくゴールする
```

ことを優先する。

---

# 16. 安全性

自律走行中に以下の異常が発生した場合、必ず安全側に遷移する。

* センサ値異常
* エンコーダ異常
* モータ制御異常
* 通信異常
* 制御周期異常
* 姿勢異常
* 予期しない障害物
* ソフトウェア例外

基本動作:

```text
異常検出
    ↓
モータ停止
    ↓
ERROR / EMERGENCY_STOP
```

マイクロマウスのアルゴリズムよりも安全停止を優先する。

---

# 17. デバッグ機能

実装中は、以下の情報を確認できるようにする。

## 17.1 迷路表示

例:

```text
+---+---+---+
|   |       |
+   +---+   +
|       |   |
+---+---+---+
```

または、テキスト以外の既存の表示方式を利用する。

最低限、以下を確認できること。

* 現在位置
* 現在方向
* 壁情報
* 訪問済みセル
* ゴール
* 次の目標セル
* 距離マップ

## 17.2 走行ログ

以下をログに記録する。

```text
timestamp
state
maze position
direction
sensor values
wall detection
velocity
angular velocity
encoder position
gyro angle
next action
```

可能な限り、再現可能なデバッグログを作る。

---

# 18. テスト方針

テストは以下の3層に分ける。

## 18.1 Unit Test

ハードウェアなしで実行可能なテスト。

対象:

* Maze
* Wall
* Position
* Direction
* BFS
* Flood Fill
* Path Planner

## 18.2 Simulation Test

センサとモータをモックして実行する。

例:

```text
仮想迷路
    ↓
仮想センサ
    ↓
マイクロマウスアルゴリズム
    ↓
仮想走行
```

## 18.3 Hardware Test

実機で実行する。

順序:

```text
モータ単体
    ↓
直進
    ↓
旋回
    ↓
センサ
    ↓
1セル走行
    ↓
複数セル走行
    ↓
簡単な迷路
    ↓
未知迷路
```

---

# 19. 実装上の重要な制約

## 19.1 既存機能を壊さない

Daylightに既に存在する手動操作機能を壊してはならない。

マイクロマウス機能は、既存の手動操作機能と共存できる構造にする。

例:

```text
Manual Mode
Autonomous Mode
Micromouse Mode
```

のように、明確にモードを分離する。

---

## 19.2 ハードウェア依存コードを分離する

迷路アルゴリズムは、可能な限りハードウェアから独立させる。

悪い例:

```text
if gpio_17_is_high() {
    maze[x][y].wall = true;
}
```

良い例:

```text
let observation = wall_sensor.read();
maze.update(robot_pose, observation);
```

このように、

```text
Hardware
    ↓
Sensor abstraction
    ↓
Maze algorithm
```

という依存関係にする。

---

## 19.3 TwilightとDaylightの差異を文書化する

移植した機能について、以下を記録する。

```text
Twilight implementation
Daylight implementation
Reason for difference
Hardware difference
Behavioral difference
```

特に、単純なコピーではなく再設計した部分は、なぜそうしたのかをコメントまたはドキュメントに残す。

---

# 20. 完了条件

以下をすべて満たした時点で、基本的なマイクロマウス機能の実装完了とする。

### ソフトウェア

* [ ] Mazeデータ構造が実装されている
* [ ] 壁情報を管理できる
* [ ] 壁情報の隣接セル整合性が保たれる
* [ ] 現在位置を管理できる
* [ ] 現在方向を管理できる
* [ ] ゴール判定ができる
* [ ] 探索アルゴリズムが動作する
* [ ] 最短経路を計算できる
* [ ] 最短経路を走行命令に変換できる
* [ ] 状態機械が実装されている

### ハードウェア

* [ ] 壁センサを取得できる
* [ ] エンコーダを取得できる
* [ ] モータを制御できる
* [ ] 直進できる
* [ ] 左旋回できる
* [ ] 右旋回できる
* [ ] 180度旋回できる
* [ ] セル単位で走行できる

### 実機

* [ ] 既知の簡単な迷路を走行できる
* [ ] 未知の壁を探索できる
* [ ] 迷路情報を更新できる
* [ ] ゴールに到達できる
* [ ] 探索結果から最短経路を計算できる
* [ ] 最短経路を再走行できる
* [ ] 異常時に安全停止できる

---

# 21. AIエージェントへの最終指示

このタスクでは、いきなり実装を開始してはならない。

まず以下を実行すること。

1. `daylight` の全体構成を調査する
2. `robosweep_twilight` のマイクロマウス走行に再利用可能なコードを調査する
3. DaylightとTwilightのハードウェアおよびソフトウェアの差分を整理する
4. 既存のDaylight機能を再利用できる部分を特定する
5. 実装計画を作成する
6. その後、最小単位から実装する

実装中は、常に以下の優先順位を守る。

```text
正確性
    >
安全性
    >
保守性
    >
テスト可能性
    >
性能
```

最初から高速走行を目指さない。

まず、

> Daylightが、自律的に迷路を認識し、セル単位で走行し、ゴールに到達できる

状態を完成させること。

その後に、

> 走行速度の向上、軌道最適化、スラローム、高速最短経路走行

を段階的に追加すること。

Twilightは参考実装である。

しかし、最終的な設計の正しさは、Twilightのコードをどれだけコピーしたかではなく、

> Daylightのハードウェアとソフトウェアアーキテクチャに適した、テスト可能で保守可能なマイクロマウス機能として実装できているか

によって判断すること。
