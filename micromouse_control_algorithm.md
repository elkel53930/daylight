# Daylight マイクロマウス制御アルゴリズム

2026-07-19 の実走行で、探索は 6 つの判断点を通ってゴール (0,2) まで完走し、帰還は最初の 1 セルで止まった。
ログを読むと、半セル 90mm を前進するあいだに機体は 54° 回っている。
毎秒 90° で回転しながら壁センサを読み、三方すべてを壁と判定して、地図上の (0,1) を袋小路として記録していた。

車輪速度、直進の操舵、旋回の整定。
個々のループはこの日までの調整でそれぞれ安定になっていた。
個々のループが安定でも走行全体は壊れたのだから、壊れた場所はループの中ではなく、ループとループのあいだにあるはずである。
この文書は、その「あいだ」を特定できる粒度で現在の制御アルゴリズムを記述し、末尾で改良方針の候補を比較する。

対象はブランチ `micromouse_hw_test` の `software/mob`(ESP32-S3 ファームウェア)と `software/micromouse`(Raspberry Pi 側アプリ)である。
数値はすべて 2026-07-19 時点のコードと走行ログ `logs/run_20260719_100253.jsonl` から取った。

## 制御の二層構造

制御は二つの層に分かれている。
**mob**(ESP32-S3)が 1kHz の実時間制御をすべて持ち、Raspberry Pi 側の Python は判断点ごとにシリアルコマンドを 1 つ送って `DONE` を同期で待つ。
リンクは `/dev/ttyUSB0` の 3,000,000bps、コマンドも応答も CSV 行である。

| コマンド | 動作 | 応答 |
|---|---|---|
| `FWD,v,a,d` | 台形加速で d mm 前進(到達しても停止しない) | `DONE` |
| `STOP,v,a,d` | d mm 先で減速停止 | `DONE` |
| `TURN,rad` | その場旋回(正 = 左回り) | `DONE` |
| `QSTP` | 最大減速度で停止 | `QSTPDONE,残距離` |
| `GCAL` / `RDST` / `RANG` | ジャイロ校正 / 距離・角度リセット | `DONE` |
| `SEN` | センサ 1 フレーム要求 | `SEN,...`(11 フィールド) |
| `MOT,r,l` | 車輪速度の直接指定 | なし |

分かれているのはコードだけではない。
知識も分かれている。
mob は迷路も自分の方位も知らず、「今の一動作」の目標だけを持つ。
Python は迷路と方位(NORTH/EAST/SOUTH/WEST の 4 値)を持つが、機体の実姿勢は `SEN` で一瞬覗くだけで、制御には使っていない。
つまり、機体が迷路の格子に対して何度傾いているかを継続的に管理している場所は、どちらの層にもない。

## 実時間ループとオドメトリ

mob の Core0 は 1ms タイマ割り込みのカウンタをスピン待ちし、毎 tick 次の順で処理する(`mob.ino` の `Core0RealtimeTask`)。

1. `sensors.update()`(IMU、壁センサ、電圧、エンコーダ、オドメトリ)
2. コマンドキュー処理(各 `handleXxx` は他のプロファイルのフラグを落とすので、走行プロファイルは常に排他)
3. アクティブなプロファイルの更新(`updateForward` / `updateStop` / `updateTurn` / `updateQstp` など)
4. `motion.update()`(車輪速度制御)

オドメトリは 2 つの量を積算する(`sensors.cpp`)。

- **距離**:左右エンコーダ(AS5047、14bit)の 1ms 差分の平均。
- **角度**:ジャイロ(LSM6DSR z 軸)角速度の積分。

エンコーダ差からの角度も毎 tick 計算されているが、使われずに捨てられる(`sensors.cpp:74-77`、相補フィルタは未実装)。
角度の基準がジャイロ積分だけなので、オフセット誤差は時間に比例して、スケール誤差は回った角度に比例して蓄積し、それを検出・修正する経路は `RANG` によるゼロクリアしかない。
`GCAL` は静止 100 サンプル(5ms 間隔、約 0.5 秒)の平均をオフセットとして引くだけで、スケールと温度ドリフトは補正しない。

## 車輪速度制御

最内ループは左右独立の車輪速度制御である(`motion_controller.cpp` の `apply_speed_pid`、1kHz)。

速度の測定は 1ms のエンコーダ差分から行う。
1ms 差分は量子化と読み取りタイミングの揺れでノイズが大きいため、EMA フィルタ(α = 0.12、時定数約 8ms)を通す。
α = 0.3 ではジャイロと矛盾する ±100mm/s 級のスパイクが残った(07-19 実測)。

出力 duty(±1023)は**フィードフォワード**と PID の和で決める。

```
duty = KF × (8.0V / vbatt) × v_ref + PID(v_ref − v_filt)
KF = 250 duty/(m/s)     (実測 duty100 ↔ 370mm/s @7.5V から同定)
KP = 300, KI = 3000, KD = 0(積分項は出力範囲でクランプ)
```

電圧でスケーリングするのは、モータの実効ゲインが電圧に反比例し、計測のたびに duty と速度の関係が ±10% 以上ずれた原因が電圧変動だったからである(vbatt < 6V は起動直後のガードとして公称 8.0V に置換)。
07-19 以前は KP=800 の実質 P 制御で、プラントゲイン約 3.7mm/s per duty に対しループゲイン約 2.4 となり振動していた。

左右目標がともに 0 のときは PID をリセットして duty 0 を出すだけで、位置保持はしない。
旋回もこの同じループの上に載っており、`turn_in_place()` は左右に ±s の速度目標を置くだけである。

## 前進走行の制御(FWD)

`FWD` は速度指令を加速度 a で目標速度 v までスルーさせる台形プロファイルで、距離 d に到達した時点で `DONE` を返す。
停止はしない。
次の `FWD` や `STOP` に切れ目なく接続するための仕様である。

距離の管理には**累積目標距離**(`cumulative_goal_dist_mm`)を使う。
コマンドごとに「今の位置 + d」を目標にするのではなく、`RDST` 以降の指令距離の総和を絶対目標とする。
`DONE` 判定の瞬間に機体が数 mm 走り過ぎていても、その分は次のコマンドの残距離から自動的に差し引かれるので、距離の誤差はコマンドをまたいで累積しない。
これはよくできた仕掛けだと思う。
では、角度の側はどうなっているか。

`FWD` を受信した瞬間、mob は `fwd_target_angle_rad = sensors.get_angle()` とする(`mob.ino:329`)。
つまり「受信時点の実姿勢」がその区間の直進の定義になる。
機体が格子に対して傾いた状態で `FWD` が始まれば、その傾きごと維持するのが正しい動作である。
距離にあった清算の仕掛けは、角度には存在しない。

走行中の操舵は 3 つの項の和で行う(1kHz)。

- **角度フィードバック**:`0.5 (m/s)/rad × (現在角 − 目標角)`。
- **角速度フィードバック**:`0.04 (m/s)/(rad/s) × gyro_z`(減衰項)。
- **壁補正**:横ずれを目標角の書き換えで直す(後述)。

和 `lateral_correction` は `motion.forward(v, corr)` を経て左右の車輪速度目標 `v ∓ corr` に展開される。

壁補正は他の 2 項と性格が違う。
横センサ値と中央目標値(ls=311 / rs=229、セル中央実測)との誤差に比例した量を、毎 tick `fwd_target_angle_rad` から引く(`mob.ino:656`)。
ゲインは 2e-6 rad/tick per unit で、両壁が見えるとき(しきい値 100 以上)は左右誤差の平均、片壁のときはその誤差をそのまま使い、残距離 30mm 未満では切る。
1kHz で積算されるので、これは実質的に積分器である。
誤差 100 なら目標角は 0.2 rad/s で動く。
片壁でセンサが飽和(4095)すれば 7.6 rad/s に達する。
上限はなく、積算した書き換え量を元に戻す項もない。
横ずれという情報を方位の基準に不可逆に混ぜ込む構造であり、補正が正しく効いた場合でも「格子に対する方位」は少しずつ失われていく[^telemetry]。

[^telemetry]: `updateForward` は 20Hz で `#V,cmd,vr,vl,ur,ul,gz,ang,rem` のテレメトリを吐く。07-19 のチューニングはすべてこの記録に基づく。運用で不要になったら削除かフラグ化を検討する(残タスク)。

## 停止の制御(STOP と QSTP)

`STOP` は残距離に応じた三相の減速で止まる(`updateStop`)。

1. 余裕があるうちは巡航速度を維持(必要なら加速)。
2. 残距離が減速所要距離に入ったら 50mm/s(最終進入速度)へ減速。
3. さらに詰まったら減速を続けるが、40mm/s 未満には落とさない。静止摩擦でモータが目標手前で止まるのを防ぐためである(20mm/s では実機がスタックした)。
4. 残距離 0 で `motion.stop()`(即 duty 0)して `DONE`。

40mm/s から瞬時に 0 へ落とすため、停止の瞬間に小さな衝撃が出る。
「停止・発進のギクシャク」として観察されている症状の一因で、未改修である。
4 秒で止まりきらない場合は異物への押し付けとみなし、30mm 後退してから `DONE` を返す。
`STOP` 中の操舵は角度と角速度のフィードバックのみで、壁補正は行わない。

`QSTP` は中断用で、現在の指令速度から 1000mm/s² で減速し、5mm/s 以下で停止して元目標までの残距離を `QSTPDONE` で返す。
なお `QSTP` の減速中は壁補正が有効で、`STOP` と非対称になっている(意図した差かどうかはコードからは読み取れない)。

## その場旋回の制御(TURN)

`TURN,rad` は相対指令である。
受信時に `turn_goal = 現在角 + 指令角` を確定し、以後は残角に対するサーボになる(`updateTurn`)。

- 残角の P 制御(0.35 (m/s)/rad)で車輪速度を決め、0.06〜0.25 m/s にクランプ、1.2 m/s² のスルーレート制限をかける。
- 残角が許容(0.01 rad ≈ 0.57°)に入ったら一旦停止し、**整定確認**として 80ms 待って慣性で流れた分を再測定する。
- 許容内なら `DONE`、外れていれば再サーボ(最大 3 回)。

下限速度 0.06 m/s は終端の粘り対策(0.04 では遅すぎた)、整定確認は「許容に入った瞬間に `DONE` を返すと慣性で 1.2〜1.8° 流れる」問題への対策で、導入後の相対誤差は最大 0.6° まで下がった。
これで旋回の問題は片づいた、と 07-19 の昼の時点では考えていた。
だが「指令された相対角を正確に回る」ことと「迷路の格子に正対する」ことは別である。
基準は旋回開始時の現在角なので、開始時に方位誤差があれば、0.6° の精度でそっくり保存される。

## セル走行の幾何(Python 層)

Python 側で迷路座標系を mm と rad に翻訳するのは `cell_runner.py` だけである。
探索の**判断点**はセル境界に置かれ、機体はそこで進入先セルの壁を読む。
セル長 180mm、半セル 90mm、探索速度 300mm/s、加速度 1000mm/s² が現在の設定である。

| アクション | コマンド列 |
|---|---|
| スタート | FWD 90mm(セル中心 → 最初の境界) |
| 直進 | FWD 90mm × 2(境界 → 次の境界) |
| 旋回 | STOP 90mm(中心停止)→ TURN ±π/2 or π → FWD 90mm |
| ゴール到達 | STOP 90mm(中心停止) |

直進を半セル 2 回に割るのは Twilight 実績の踏襲で、通信遅延があっても累積目標距離が半セル単位で確定するためである。

方位の管理はここでも 4 値である。
足立法が返す絶対方位を `relative_action()` で fwd/left/right/back に落とし、旋回は常に固定量(±π/2、π)の相対 `TURN` として送る。
`SEN` に含まれる `odo_ang` はログに記録されるだけで、指令の補正には使われていない。

## 判断点での観測と地図更新

観測は探索ループの各周回の頭、直前の `FWD` の `DONE` 直後に行う(`state_machine.py` の `_observe_and_update`)。
`FWD` は停止しないから、観測は約 300mm/s で走行中の 1 ショットである。
`SEN` 1 フレームをしきい値(front = min(lf,rf) ≥ 80、ls ≥ 100、rs ≥ 100)で 3 方向の壁有無に変換し、機体の論理方位で絶対方位に回して迷路へ書き込む。

この手順は 3 つの前提の上に立っている。
機体が境界にいること、格子に正対していること、セル中央線上にいること。
どれも検証されない。
`SEN` には gyro と odo_ang が含まれているのに、判定に使うのは壁の 3 値だけである。
書き込み側の `observe_wall()` も無条件で、既知の壁情報を新しい観測で常に上書きする。

地図更新後は**足立法**で次の方位を決める。
ゴール群への距離マップを毎回 BFS で再計算し(探索中は未知壁 = 通行可の楽観)、距離が最小の隣接セルへ、同着なら直進優先で進む。
現在セルの距離が INF なら "no path to goal" として安全停止し ERROR となる。

失敗した走行の 6 回の探索観測は、実は全部が回転しながらの読み取りだった。
観測時の gyro は順に 0.78、0.24、0.40、0.49、0.70、−0.03 rad/s。
平均すれば毎秒 25° で首を振りながら読んでいたことになる。
帰還の 1 回だけが特別な条件で失敗したのではなく、慢性的な首振りの振幅が、あの 1 回だけ許容を超えたのである。

## 帰還走行が失敗するまでの誤差伝搬

ログの各判断点で、論理方位の名目角と実測 odo_ang を並べる(NORTH = 0、左回り正)。

| 判断点 | 方位 | 名目 [rad] | 実測 [rad] | 差 |
|---|---|---|---|---|
| (0,1) | NORTH | 0 | −0.05 | −2.9° |
| (1,1) | EAST | −1.571 | −1.71 | −8.0° |
| (1,2) | NORTH | 0 | −0.11 | −6.3° |
| (1,3) | NORTH | 0 | +0.07 | +4.0° |
| (0,3) | WEST | +1.571 | +1.69 | +6.8° |
| (0,2) | SOUTH | π | +2.76 | −21.8° |
| (0,1) 帰還 | SOUTH | π | +3.70 | +32.1° |

最後の 2 行のあいだに起きたことを、ここまでの各節の部品で組み立てられる。

(0,3) で左旋回した直後の目標角は約 3.26 rad のはずだが、次の境界では 2.76 rad まで落ちている。
半セルの前進で −29° 回った計算で、旋回精度 0.6° では説明がつかない。
壁補正の積分が目標角を動かしたと考えるのが最も整合的である(壁への軽い接触の可能性はログからは排除できない)。
ゴールセルの `STOP` はこの 2.76 rad を保持して中心に止まった。

帰還の最初のアクションは直進(SOUTH のまま)なので旋回による矯正の機会はなく、`FWD` は −22° 傾いた姿勢をそのまま「まっすぐ」として採用した。
格子に対して 22° 傾いて 180mm 幅の通路を走ると、横センサは壁を斜めかつ至近で見て値が跳ね上がる。
センサ値が大きいほど壁補正の積分は速く回り、角度フィードバックがその動く目標を追って機体を実際に回すから、傾きはさらに深くなる。
片壁分岐に入れば係数 0.5 も外れる。
半セル走り終えた境界で機体は +54° 回り込み、なお 1.57 rad/s で回転中だった。

その姿勢では前センサが向くのは (0,1) の南壁ではなく西側壁の至近で、294 という値はしきい値 80 を大きく超える。
横センサも同様に壁を拾い、観測は「三方すべて壁」となった。
(0,1) の南、東、西が壁として記録されると、(0,0) への唯一の入口(南)が塞がるので、距離マップ上 (0,0) は到達不能になり、"no path to goal from (0, 1)" で走行が終わった。

上書き方式の副作用も 1 つ記録に残っている。
最初の観測(gyro 0.78 rad/s)は (0,1) の北壁を front=122 で壁と誤記録しており、のちに (0,2) からの観測(front=72)が開放と上書きして静かに直していた。
無条件上書きは誤りを直すこともあれば、正しい記録を壊すこともある。
帰還では後者が起きた。

冒頭の問いに答えられる。
機体が回ったのは、`FWD` が開始時の姿勢を無条件に直進の基準として採用し、壁補正が上限も戻しもない積分でその基準を書き換え続け、蓄積した方位誤差を清算する機構がどの層にもないからである。
回りながらの値が地図に書かれたのは、観測に静止と正対の検証がなく、書き込みが無条件上書きだからである。
距離には累積目標距離という清算の仕掛けがあった。
角度と横位置には、それに相当するものがない。

## 改良方針の候補

| # | 案 | 層 | 防ぐもの | 残るもの |
|---|---|---|---|---|
| 1 | 観測のゲーティング | Python | 回転中の誤観測 | 方位誤差の蓄積 |
| 2 | 方位の絶対化 | Python | 方位誤差の蓄積 | ジャイロ積分自体の誤差 |
| 3 | 壁補正の再設計 | mob | 目標角の暴走 | 観測前提の未検証 |
| 4 | 境界での静止観測 | Python | 回転中の誤観測 | 探索時間の増加(副作用) |
| 5 | 前壁リファレンス | 両方 | 蓄積誤差の残り | 前壁がない区間 |

**観測のゲーティング**(案 1)は、`SEN` にすでに含まれる gyro と odo_ang を判定に使う。
|gyro| が閾(0.3 rad/s 程度)を超えるか、odo_ang が期待方位から大きく外れていたら、その観測を棄却して静止再読するか、走行を止めてエラーにする。
変更は `_observe_and_update` の周辺だけで済み、シミュレータ回帰にも影響しない。
症状(地図汚染)は防ぐが、原因(方位誤差)は残る。

**方位の絶対化**(案 2)は、Python が格子上の期待方位角(0、±π/2、π の累積)を持ち、旋回のたびに `TURN,名目 + (期待 − odo_ang)` を送る。
旋回ごとに蓄積誤差が清算されるので、`FWD` が採用する開始姿勢も毎回格子へ引き戻される。
累積目標距離が距離でやっていることの、角度版である。
mob 側の変更は不要で、直進アクションが続く区間では清算が入らない点だけ弱い(必要なら n 直進ごとに 0 rad の `TURN` を挟めば同じ機構で清算できる)。

**壁補正の再設計**(案 3)は mob 側で、積分にリークを入れて目標角を `FWD` 開始角へ緩やかに戻す、書き換え量に上限を設ける、あるいは積分をやめて横位置フィードバックとして `lateral_correction` に直接加える、のいずれかになる。
斜め進入でセンサ値が跳ねたときに目標角が飛ぶ正帰還を断てるが、パラメータの再チューニングと実機検証が必要で、変更コストは Python 側 2 案より高い。

案 4 は確実だが常時のコストが高く、案 1 の「異常時だけ静止再読」で足りるなら不要である。
案 5 は前壁があるセルで距離と方位を較正する将来拡張で、上記が入って初めて効果を分離測定できる。

組み合わせとしては、まず案 2 で蓄積を断ち、案 1 を安全網として重ねるのが、変更量に対して失敗経路を最も広く塞ぐ。
案 3 は案 2 導入後の実測で壁補正起因の暴れが残った場合に着手すればよい。

次の走行で最初に見るべき数値は、判断点ごとの odo_ang と名目方位の差である。
これが ±数度に収まっていれば、帰還はあの半セルを普通に通過するはずである。
一方、ジャイロ積分そのものの信頼性(スケール誤差と長時間ドリフト)は、方位の清算を入れて初めて他の誤差源から分離して測れるようになる。
16×16 の実大会迷路で何分の探索に耐えるかは、その測定が済むまで開いたままの問いである。
