# マイクロマウス実機検証 進捗記録(2026-07-18)

`micromouse_hw_test` ブランチでの実機段階検証(`hw_test.py`)の進捗と残タスク。
実装の経緯は `micromouse_implementation_report.md`、開発の前提知識は `CLAUDE.md` を参照。

## 検証ステージの状況

| ステージ | 結果 | 実測値 |
|---|---|---|
| `sen` | ✅ 合格 | 11フィールド応答、gyro≈0、値安定 |
| `walls` | ✅ 合格 | 下記「壁センサ校正」参照 |
| `gcal` | ✅ 合格 | 校正後ドリフトほぼゼロ(6秒で0.01rad以下) |
| `fwd` | ✅ 合格 | 180.5mm / +0.01rad(目標180mm) |
| `turn` | ✅ 合格 | 左+88.24° / 右-88.81°(目標±90°) |
| `cycle` | ⬜ **未実施** | 次回はここから |

## 今回のセッションで解決した問題

### 1. Makefile の upload が古いキャッシュを書き込むバグ(重要)
`make upload` は `.build` にコンパイルするのに、`arduino-cli upload` に
`--input-dir` を渡していなかったため、`~/.cache/arduino/sketches/` の
**古いビルドを書き込んでいた**。「書き込み成功に見えるのに挙動が変わらない」
場合はまずこれを疑う(症状: esptool が全パーティションで
"No changed sectors found" を表示)。→ `--input-dir $(BUILD_PATH)` を追加して修正済み。
あわせて書き込みのみの `make upload-only` を追加(ビルドを忘れると古い
バイナリが載る点は注意)。

### 2. 前後方向の反転
初回 fwd で機体が後退した。前進の定義を反転するため **3箇所をセットで修正**:
- `motor.cpp` — 方向ピン: 正速度=LOW(CCW)、負速度=HIGH(CW)
- `motion_controller.cpp` — 速度PID用エンコーダ距離の符号(右:正、左:負に)
- `sensors.cpp` — オドメトリ用エンコーダ距離の符号(同上)

⚠️ モータ極性だけ反転するとエンコーダ測定速度が指令と逆符号になり、
車輪速度PIDが正帰還で暴走する。必ず3箇所同時に変更すること。
ジャイロ(旋回方向)は機体の向き自体が変わらないので変更不要。

### 3. STOP の最終進入スタック
fwd が目標0.7mm手前でスタック→4秒タイムアウト→30mm後退
(`STOP_BACKOFF_DIST_MM`)し、odo が約150mmになる症状。
最終進入の最低速度 `STOP_MIN_SPEED_MMPS` 20mm/s では静止摩擦に負けるため
**40mm/s に引き上げて解消**。odo が「目標-30mm」付近ならこのタイムアウト
後退を疑う。

### 4. TURN の挙動understanding(バグではない)
- 旋回完了は `TURN_DONE_TOL_RAD = 0.03rad(約1.72°)` の許容内に入った瞬間に
  DONE を返す決定的動作のため、odo angle は毎回ほぼ同一値
  (左は常に+88.24°)になる。仕様であってバグではない。
- 「左90°→右90°」を連続実行すると元の向きに戻るため、動いていないように
  見誤りやすい。

## 壁センサ校正(2026-07-18 実測)

機体をセル境界に置いた実測値:

| センサ | 壁なし | 壁あり |
|---|---|---|
| 左横 `ls` | 17〜25 | 294〜311 |
| 前 `front` | 5〜15 | 99〜111 |
| 右横 `rs` | 12〜20 | 217〜232 |

分離が十分なため、しきい値は既定値のまま **L=100 / R=100 / F=50** とした。
`software/micromouse/config/micromouse.yaml` に記録済み(gitignore対象の
機体固有ファイル。消えた場合は上記の値で再作成)。

## 環境の状態(セッション終了時点)

- ブランチ: `micromouse_hw_test`(master をマージ済み、GEAR_RATIO=1.0 を含む)
- ESP32 ファーム: 上記修正をすべて含むビルドを書き込み済み
  (SEN 11フィールド応答を確認済み)
- `software/venv` に pyserial 導入済み(2026-07-18、ユーザー実行)
- ジャイロ校正(gcal)実施済み — ただし**電源再投入後は再実行が必要**
- ⚠️ バッテリー: 6.85V まで低下(カットオフ 6.5V)。**次回は充電してから**

## 残タスク

1. **`hw_test.py cycle`** — 探索1サイクル(半セル90mm前進→判断点で壁読み→
   半セル90mmでセル中心停止)。「判断点で左右どちらかに壁がある」配置にし、
   壁検出の True/False が実配置と一致するかを確認する。
2. **旋回精度の要否判断** — 左右とも約1.5°のアンダーシュート(許容誤差由来)。
   実走行では壁補正が掛かるためまず実走で評価し、必要なら
   `TURN_DONE_TOL_RAD` の縮小や DONE 後の整定待ちを検討。
3. **実走行(探索)テスト** — cycle 合格後、`micromouse_app.py` での
   迷路探索。走行前にシミュレーション回帰(`--sim`、CLAUDE.md 参照)。
4. **master へのマージ判断** — 実走行が安定したら `micromouse_hw_test` を
   master にマージする。
5. (任意)`software/mob/README.md` の SEN 記述が古いまま(9フィールド)。
   11フィールドの現状に更新する。
