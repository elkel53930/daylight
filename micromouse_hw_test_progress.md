# マイクロマウス実機検証 進捗記録(最終更新: 2026-07-19)

`micromouse_hw_test` ブランチでの実機段階検証(`hw_test.py`)の進捗と残タスク。
実装の経緯は `micromouse_implementation_report.md`、開発の前提知識は `CLAUDE.md` を参照。

## 検証ステージの状況

| ステージ | 結果 | 実測値 |
|---|---|---|
| `sen` | ✅ 合格 | 11フィールド応答、gyro≈0、値安定 |
| `walls` | ✅ 合格 | 下記「壁センサ校正」参照 |
| `gcal` | ✅ 合格 | 校正後ドリフトほぼゼロ(6秒で0.01rad以下) |
| `fwd` | ✅ 合格 | 180.0〜180.9mm / ±0.00rad(目標180mm) |
| `turn` | ✅ 合格 | 最大誤差0.6°、5/6回が±0.05°(整定確認導入後) |
| `cycle` | ✅ 合格 | 壁検出L/F/R一致、セル中心停止(2026-07-19) |
| 実走行 | ✅ 合格 | 探索→ゴール(0,2)→帰還→最短経路計画まで完走(2026-07-19) |

## 2026-07-19 セッションの成果

### 1. バッテリー電圧の補正(コミット adc3132)
`analogRead` の線形換算(誤差-11%)を `analogReadMilliVolts()`(eFuse校正)
に置き換え。誤差約1%に改善。

### 2. 前壁しきい値の校正(config/micromouse.yaml、git管理外)
スタート位置(前壁なし)で front=39〜61 を観測し、しきい値50では
1〜2セル先の壁の反射を誤検出して "no path to goal" になった。
**wall_front_threshold: 50→80** に変更(壁あり実測99〜111との中間)。
テスト迷路用にゴールを (0,2) 単一セルに設定
(goal_x_min/max: 0, goal_y_min/max: 2)。

### 3. OLEDエラー表示(コミット fb7ab70)
ERROR終了時に `mission.error_message` を最大2行OLED表示。

### 4. 走行チューニング一式(コミット 2442b28)
テレメトリ(#VEL、20Hz)を追加し、データに基づいて改修:

| 症状 | 原因 | 対策 |
|---|---|---|
| 直進の蛇行 | 壁補正の中央目標値が実測と乖離(LS 233 vs 実測311)。1kHzで目標角度に積算され常時操舵 | 目標値をセル中央実測(311/229)に再校正 |
| ヨー振動(GZ±0.6rad/s) | 壁補正積算が速すぎ角度ループと干渉 | 壁補正ゲイン5e-6→2e-6、角速度FB 0.01→0.04 |
| 旋回角ばらつき(誤差1.2〜1.8°) | 許容0.03radに入った瞬間DONE、慣性流れ | 許容0.01rad+整定確認(80ms→再サーボ最大3回)。最低速度0.04→0.06m/s |
| 速度サージ | KI=60で実質P制御+KP過大(ループゲイン2.4)で振動、電圧変動が直撃 | FF+PID化: FF=250duty/(m/s)×(8.0V/vbatt)、KP300/KI3000、アンチワインドアップ、速度LPF α=0.12 |

計測ログ: セッションのスクラッチパッド `vel_capture_01〜06.log`(一時領域のため
消える。必要なら再計測: `capture_vel.py` 方式 = 生pyserialでGCAL→RDST→RANG→
FWD,300,1000,90→STOP,300,1000,90 を送り全行記録)。

### 実測で得た機体特性(2号機)
- プラントゲイン: duty100 ↔ 約370mm/s(vbatt≈7.5V)
- 巡航duty: 約80〜100(300mm/s時)
- 壁センサ セル中央実測: ls=299〜322 / rs=216〜242、前壁なし時のfront=0〜17
  (スタート位置など前方1〜2セル先に壁がある場合は39〜61まで上がる)

## 壁センサ校正(2026-07-18 実測、07-19 一部改訂)

機体をセル境界に置いた実測値:

| センサ | 壁なし | 壁あり |
|---|---|---|
| 左横 `ls` | 17〜25 | 294〜311 |
| 前 `front` | 5〜15(遠方に壁があると〜61) | 99〜111 |
| 右横 `rs` | 12〜20 | 217〜232 |

しきい値: **L=100 / R=100 / F=80**(F は 07-19 に 50→80 へ変更)。
`software/micromouse/config/micromouse.yaml` に記録済み(gitignore対象の
機体固有ファイル。消えた場合は上記の値+ゴール設定で再作成)。

## 環境の状態(2026-07-19 セッション終了時点)

- ブランチ: `micromouse_hw_test`
- ESP32 ファーム: 上記チューニングをすべて含むビルドを書き込み済み
- ジャイロ校正は**電源再投入後は再実行が必要**(アプリは走行開始時に自動実行)
- 実走行はゴール(0,2)のテスト迷路で探索→帰還→最短計画まで確認済み

## 残タスク

1. **速度ムラの残り** — 「許容範囲だが完全ではない」(ユーザー評価 2026-07-19)。
   さらに詰める場合は #VEL テレメトリで巡航区間を再計測し、
   FFゲイン微調整 or KI増強を検討。
2. **停止・発進のギクシャク再評価** — STOP の最終進入
   (50mm/s→最低40mm/s貼り付き→即0)は未改修。実走行で気になるようなら
   減速プロファイルの見直し。
3. **最短走行(speed run)の実機確認** — 探索・帰還・経路計画までは確認済み。
   speed run 実行と、その際の速度(400mm/s)での安定性確認。
4. **master へのマージ判断** — 実走行が安定したら `micromouse_hw_test` を
   master にマージする。
5. (任意)`software/mob/README.md` の SEN 記述が古いまま(9フィールド)。
   11フィールドの現状に更新する。
6. (任意)#VEL テレメトリはデバッグ用。運用で邪魔なら削除 or フラグ化。
