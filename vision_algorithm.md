# カメラ画像によるボール・壁エッジ検出機能 実装指示書

## 1. 目的

Raspberry Piに接続されたRaspberry Pi Camera Module 3 Wideから取得した画像を用いて、以下の視覚認識機能を実装する。

1. 黄色いボールの存在検出
2. 黄色いボールの中心座標および直径の推定
3. 赤色のマイクロマウス壁上面のエッジ検出

画像処理関数は、カメラの解像度に依存しないように実装する。

入力画像の解像度は、例えば以下を想定する。

* QVGA: `320 x 240`
* VGA: `640 x 480`
* HD: `1280 x 720`
* FullHD: `1920 x 1080`

ただし、これらに限定しない。

---

# 2. 入力画像

## 2.1 画像形式

各画像処理関数の引数は、以下の形式とする。

```python
numpy.ndarray
```

画像はPicamera2から取得した **BGR形式** とする。

例:

```python
image: np.ndarray
```

画像の形状は通常以下である。

```python
(height, width, 3)
```

色チャンネルの順序は以下。

```text
image[:, :, 0] = B
image[:, :, 1] = G
image[:, :, 2] = R
```

OpenCVを使用する場合、BGR形式をそのまま扱える。

---

# 3. 共通要求事項

## 3.1 任意解像度への対応

画像処理関数は、画像の解像度を固定してはならない。

以下のようなコードは禁止する。

```python
image[0:240, 0:320]
```

画像サイズに依存する処理が必要な場合は、必ず以下から動的に取得する。

```python
height, width = image.shape[:2]
```

解像度に依存する閾値については、可能な限り画像サイズに対する相対値として扱う。

例えば、

```text
画像中の黄色ピクセル数 > 100
```

ではなく、

```text
黄色ピクセルの割合 > threshold
```

を基本とする。

ただし、ボール検出の仕様上、絶対ピクセル数が必要な場合には、基準解像度に対してスケーリングすること。

---

# 4. HSV色範囲設定

黄色および赤色の判定にはHSV色空間を使用する。

BGR画像をHSV画像に変換する。

```python
hsv_image = cv2.cvtColor(image, cv2.COLOR_BGR2HSV)
```

HSVの範囲は、ソースコードにハードコードしてはならない。

外部から設定可能な構造にする。

例えば:

```python
@dataclass
class ColorRange:
    lower: tuple[int, int, int]
    upper: tuple[int, int, int]
```

または、プロジェクトの既存設定方式に合わせる。

設定例:

```python
yellow_range = ColorRange(
    lower=(h_min, s_min, v_min),
    upper=(h_max, s_max, v_max),
)
```

赤色についてはHSVの色相が0度付近と180度付近に分かれる可能性があるため、必要であれば複数のHSV範囲を指定できる設計とする。

例:

```python
red_ranges = [
    ColorRange(lower=(0, s_min, v_min),
               upper=(h_max_low, s_max, v_max)),
    ColorRange(lower=(h_min_high, s_min, v_min),
               upper=(179, s_max, v_max)),
]
```

---

# 5. 黄色いボールの存在検出

## 5.1 目的

画像中に黄色いボールが存在する可能性があるかを高速に判定する。

この処理は、後段の高精度なボール位置推定を実行するかどうかを決定するための前段処理である。

---

## 5.2 API

以下のような関数を実装する。

```python
def detect_yellow_ball(
    image: np.ndarray,
    color_range: ColorRange,
    threshold: float,
) -> bool:
    ...
```

具体的な引数名や設定クラスは、既存プロジェクトの設計に合わせてよい。

ただし、関数の意味は以下とする。

```text
入力:
    BGR画像

出力:
    True  : 黄色いボールが存在する可能性が高い
    False : 黄色いボールが存在する可能性が低い
```

---

## 5.3 処理内容

基本処理:

1. BGR画像をHSV画像に変換
2. 黄色のHSV範囲に該当するピクセルのマスクを生成
3. 黄色ピクセル数または割合を計算
4. 閾値以上であれば `True`
5. 閾値未満であれば `False`

概念的には以下。

```text
BGR image
    ↓
HSV conversion
    ↓
Yellow HSV mask
    ↓
Number of yellow pixels
    ↓
Threshold comparison
    ↓
True / False
```

---

## 5.4 高速性要求

この関数は高頻度で実行される可能性があるため、高速性を優先する。

以下を基本とする。

* NumPyによるベクトル化処理を使用する
* Pythonの全ピクセルループは禁止
* 不要な画像コピーを避ける
* 輪郭抽出は必須ではない
* RANSACや円フィッティングを実行してはならない

可能であれば、以下のような処理で実装する。

```python
mask = cv2.inRange(
    hsv_image,
    lower,
    upper,
)

yellow_pixel_count = cv2.countNonZero(mask)
```

---

## 5.5 閾値の扱い

画像解像度が異なる場合でも、同じ意味の閾値になるようにする。

推奨:

```text
yellow_pixel_ratio =
    yellow_pixel_count / total_pixel_count
```

そして、

```text
yellow_pixel_ratio > threshold
```

で判定する。

ただし、背景や照明条件によって小さな黄色ノイズが発生する場合があるため、必要に応じて以下を設定可能にする。

* 黄色ピクセル割合の閾値
* 最小黄色領域面積
* HSVの色範囲

最初の実装では、単純な黄色ピクセル数または割合による判定を基本とする。

---

# 6. 黄色いボールの中心座標・直径推定

## 6.1 目的

黄色いボールが存在すると判定された場合、画像中の黄色領域からボールの幾何学的情報を推定する。

推定する値:

* ボール中心座標
* ボール直径
* 推定結果に対する信頼度

---

## 6.2 API

以下のような関数を実装する。

```python
def estimate_yellow_ball(
    image: np.ndarray,
    color_range: ColorRange,
    config: BallEstimationConfig,
) -> BallEstimationResult:
    ...
```

戻り値は、例えば以下のような構造体またはdataclassとする。

```python
@dataclass
class BallEstimationResult:
    center_x: float
    center_y: float
    diameter: float
    confidence: float
```

単純なtupleでもよいが、可読性のためdataclassを推奨する。

例:

```python
result = estimate_yellow_ball(image)

result.center_x
result.center_y
result.diameter
result.confidence
```

---

## 6.3 座標系

中心座標は入力画像のピクセル座標系で返す。

```text
原点: 画像左上

x:
    右方向が正

y:
    下方向が正
```

例えば画像サイズが `1920 x 1080` の場合:

```text
左上     = (0, 0)
右上     = (1919, 0)
左下     = (0, 1079)
右下     = (1919, 1079)
```

中心座標および直径は、入力画像の解像度に対応したピクセル単位とする。

---

# 7. RANSACによるボール推定

## 7.1 基本方針

黄色領域から得られた点群に対して、RANSACを使用して円を推定する。

推定する円:

```text
(x - cx)^2 + (y - cy)^2 = r^2
```

最終的に以下を返す。

```text
center_x = cx
center_y = cy
diameter = 2r
```

---

## 7.2 なぜRANSACを使うか

画像中の黄色領域には以下のような外れ値が存在する可能性がある。

* 黄色いボール以外の黄色物体
* 反射光
* ノイズ
* ボール以外の黄色ピクセル
* ボールの輪郭以外の領域

そのため、すべての黄色ピクセルを単純な最小二乗法で円フィッティングするのではなく、外れ値に頑健なRANSACを使用する。

---

## 7.3 推奨処理フロー

```text
入力画像
    ↓
HSV変換
    ↓
黄色マスク生成
    ↓
黄色ピクセル座標抽出
    ↓
必要に応じて点数を制限 / サンプリング
    ↓
RANSACによる円フィッティング
    ↓
インライア判定
    ↓
円の再フィッティング
    ↓
中心・直径・信頼度を返す
```

---

## 7.4 RANSACの基本アルゴリズム

円を決定するため、最低3点をランダムに選択する。

```text
1. 黄色ピクセル群から3点をランダムに選択
2. 3点を通る円を計算
3. 全点について円周からの距離を計算
4. 距離が閾値以内の点をインライアとする
5. インライア数またはインライア率を評価
6. これを指定回数繰り返す
7. 最も良いモデルを採用
```

円周から点 `(x_i, y_i)` までの距離は、概念的には以下。

```text
distance_i =
    abs(
        sqrt((x_i - cx)^2 + (y_i - cy)^2) - r
    )
```

この距離が設定した許容誤差以内ならインライアとする。

---

## 7.5 RANSACのパラメータ

以下の値は設定可能にする。

* 最大反復回数
* 円周からの最大許容誤差
* 最小インライア率
* 最小半径
* 最大半径
* 使用する点の最大数

特にFullHD画像では黄色ピクセル数が非常に多くなる可能性があるため、RANSACに入力する点数を制限できるようにする。

例えば:

```text
黄色ピクセルが100,000点ある
    ↓
最大5,000点をランダムサンプリング
    ↓
RANSAC実行
```

ただし、サンプリングによって精度が低下しないようにする。

---

# 8. ボール推定の信頼度

## 8.1 定義

信頼度は、RANSACのインライア率とする。

```text
confidence =
    number_of_inliers / number_of_sampled_points
```

値の範囲:

```text
0.0 <= confidence <= 1.0
```

例えば:

```text
全点数       = 1000
インライア数 = 850

confidence = 0.85
```

---

## 8.2 注意事項

信頼度は単純にインライア率を返す。

ただし、以下の場合には推定失敗として扱う。

* 黄色ピクセルが少なすぎる
* RANSACに必要な点数がない
* 有効な円が得られない
* 半径が設定範囲外
* インライア率が最低閾値未満

この場合のAPI仕様は、プロジェクトの既存エラー処理方式に合わせる。

推奨例:

```python
Optional[BallEstimationResult]
```

または:

```python
BallEstimationResult | None
```

推定失敗時:

```python
None
```

を返す。

---

# 9. ボール推定の精度向上

RANSACで最良の円を選択した後、最終的なインライア点のみを使用して再フィッティングすることを推奨する。

処理:

```text
全黄色点
    ↓
RANSAC
    ↓
最良モデル
    ↓
インライア点抽出
    ↓
インライア点だけで最終円フィッティング
    ↓
最終中心・半径
```

これにより、RANSACで得られた初期モデルよりも、最終的な中心座標および直径の精度向上が期待できる。

---

# 10. 赤色の壁上面エッジ検出

## 10.1 目的

マイクロマウスの壁上面は赤色である。

画像中に複数の壁上面が写る可能性がある。

この中から、

```text
画像中で最も下側に存在する赤色エッジ
```

のみを検出する。

これは、ロボットから見て最も手前にある壁上面を意味する。

---

## 10.2 API

以下のような関数を実装する。

```python
def detect_nearest_red_wall_edge(
    image: np.ndarray,
    color_range: ColorRange,
    config: WallEdgeDetectionConfig,
) -> tuple[float, float] | None:
    ...
```

戻り値:

```text
(a, b)
```

ただし、検出された直線は以下で表す。

```text
y = ax + b
```

つまり:

```text
a = 傾き
b = y切片
```

検出失敗時は、例えば:

```python
None
```

を返す。

---

# 11. 赤色エッジ検出処理

## 11.1 基本処理フロー

```text
BGR画像
    ↓
HSV変換
    ↓
赤色マスク生成
    ↓
必要に応じてノイズ除去
    ↓
赤色領域または赤色エッジ抽出
    ↓
直線候補を検出
    ↓
各候補の画像中の位置を評価
    ↓
最も下側の候補を選択
    ↓
y = ax + b
    ↓
a, bを返す
```

---

# 12. 赤色エッジの定義

赤色領域の境界から、壁上面のエッジを検出する。

実装方法は、以下のいずれかを検討する。

## 方法A: 赤色マスクの輪郭から直線を抽出

```text
赤色マスク
    ↓
輪郭抽出
    ↓
輪郭点から直線フィッティング
```

## 方法B: 赤色マスクに対してエッジ検出

```text
赤色マスク
    ↓
輪郭 / 境界抽出
    ↓
直線検出
```

## 方法C: Hough Transform

```text
赤色領域
    ↓
エッジ抽出
    ↓
Hough Line Transform
    ↓
直線候補
```

実装時には、実際のカメラ画像および壁形状に最も適した方法を選択する。

ただし、以下の要求は必ず満たすこと。

* 複数の赤色壁上面に対応する
* 最も手前のエッジを選択する
* 最終的に `y = ax + b` の形で返す
* 垂直線は基本的に対象外とする

---

# 13. 「最も手前」の定義

画像座標では、`y` が大きいほど画像の下側である。

したがって、検出された各線分について、その線分上の代表点のy座標を計算する。

例えば線分の両端点が:

```text
(x1, y1)
(x2, y2)
```

の場合:

```text
representative_y = (y1 + y2) / 2
```

候補の中から、

```text
representative_y が最大のもの
```

を選択する。

ただし、単純に線分の中心だけを比較すると、斜めの線で誤判定する可能性がある。

そのため、可能であれば以下のような評価を検討する。

```text
candidate_score =
    線分上の一定範囲における最大y座標
```

最終的には、画像内で最も下側にある赤色エッジを選択する。

---

# 14. 直線フィッティング

検出したエッジ点群に対して直線をフィッティングする。

返却形式:

```text
y = ax + b
```

以下を返す。

```python
a: float
b: float
```

例えば:

```python
a, b = detect_nearest_red_wall_edge(image)
```

この結果は、

```python
y = a * x + b
```

として使用できる。

---

# 15. 垂直線について

`y = ax + b` では垂直線を表現できない。

したがって、エッジが垂直に近い場合は、以下のいずれかの処理を行う。

推奨:

```text
垂直線に近い場合:
    検出失敗として扱う
```

判定条件は設定可能にする。

例えば:

```text
abs(x2 - x1) < minimum_dx
```

の場合、`y = ax + b` に変換しない。

---

# 16. 解像度に関する重要な設計

検出と推定では、異なる解像度の画像を使用する。

例:

```text
高速検出:
    QVGA
    320 x 240

高精度推定:
    FullHD
    1920 x 1080
```

したがって、処理関数は画像サイズを内部に固定してはならない。

例えば、QVGAで検出した後、FullHD画像で推定する場合でも、同じ関数を使用できるようにする。

```text
QVGA画像
    ↓
detect_yellow_ball()
    ↓
True
    ↓
FullHD画像
    ↓
estimate_yellow_ball()
    ↓
高精度な中心・直径
```

---

# 17. 推奨モジュール構成

可能であれば、以下のように機能を分離する。

```text
vision/
├── __init__.py
├── color.py
├── ball.py
├── wall.py
├── types.py
└── tests/
    ├── test_color.py
    ├── test_ball.py
    └── test_wall.py
```

実際のプロジェクト構成に合わせて変更してよい。

---

# 18. 推奨データ型

## ColorRange

```python
@dataclass
class ColorRange:
    lower: tuple[int, int, int]
    upper: tuple[int, int, int]
```

---

## BallEstimationResult

```python
@dataclass
class BallEstimationResult:
    center_x: float
    center_y: float
    diameter: float
    confidence: float
```

---

## WallEdgeDetectionResult

単純なtupleでもよい。

```python
tuple[float, float]
```

意味:

```text
(a, b)
```

ただし、将来的な拡張を考える場合はdataclassも可。

---

# 19. テスト要件

単体テストを実装する。

## 19.1 黄色ボール検出

最低限、以下をテストする。

### ケース1: 黄色領域あり

```text
黄色ピクセルが閾値以上
→ True
```

### ケース2: 黄色領域なし

```text
黄色ピクセルが閾値未満
→ False
```

### ケース3: 異なる解像度

以下の画像サイズで同じ意味の結果になることを確認する。

```text
320 x 240
640 x 480
1920 x 1080
```

---

## 19.2 ボール推定

人工的な円画像を生成してテストする。

例:

```text
中心 = (500, 300)
半径 = 100
```

この画像にノイズや外れ値を追加し、RANSACで正しく推定できることを確認する。

検証項目:

* 中心座標誤差
* 直径誤差
* 信頼度
* 外れ値に対する頑健性

---

## 19.3 信頼度

以下を確認する。

```text
インライア率が高い
    → confidence が高い

インライア率が低い
    → confidence が低い
```

信頼度は必ず:

```text
0.0 <= confidence <= 1.0
```

となること。

---

## 19.4 赤色壁エッジ

複数の赤色エッジを含む人工画像を作成する。

例:

```text
上側の壁エッジ
    y = 0.1x + 100

下側の壁エッジ
    y = 0.1x + 300
```

この場合、

```text
y = 0.1x + 300
```

の方を選択する。

---

# 20. 実装時の注意事項

## 20.1 色判定をRGB値で行わない

以下は禁止。

```python
if r > 200 and g > 150:
```

必ずHSVベースの設定可能な範囲を使用する。

---

## 20.2 解像度を固定しない

以下は禁止。

```python
WIDTH = 1920
HEIGHT = 1080
```

画像サイズは入力画像から取得する。

---

## 20.3 Pythonのピクセルループを避ける

以下は禁止。

```python
for y in range(height):
    for x in range(width):
        ...
```

可能な限りNumPyおよびOpenCVのベクトル化処理を使用する。

---

## 20.4 RANSACの計算量に注意する

FullHD画像では黄色ピクセル数が多くなる可能性がある。

したがって、必要に応じて入力点数を制限する。

ただし、ランダムサンプリングによる再現性が必要な場合は、乱数シードを設定可能にする。

---

# 21. AIエージェントへの実装手順

以下の順序で実装する。

## Step 1

既存プロジェクトの構造を調査する。

確認する項目:

* Pythonのバージョン
* 依存ライブラリ
* OpenCVの有無
* NumPyの有無
* テストフレームワーク
* 既存のカメラ画像取得コード
* 既存の設定ファイル方式

既存の設計パターンを優先する。

---

## Step 2

色範囲設定の仕組みを実装する。

黄色および赤色のHSV範囲を外部設定可能にする。

---

## Step 3

黄色ボールの高速検出関数を実装する。

要件:

```text
入力:
    BGR numpy.ndarray

出力:
    bool

処理:
    HSV色判定
    黄色ピクセル数または割合の閾値判定
```

---

## Step 4

黄色ボールのRANSAC推定関数を実装する。

要件:

```text
入力:
    BGR numpy.ndarray

出力:
    中心x
    中心y
    直径
    RANSACインライア率
```

---

## Step 5

赤色壁上面エッジ検出関数を実装する。

要件:

```text
入力:
    BGR numpy.ndarray

出力:
    a
    b

線分:
    y = ax + b
```

複数の候補が存在する場合、画像中で最も下側にあるエッジを返す。

---

## Step 6

単体テストを実装する。

実画像だけに依存せず、人工画像を生成してテストする。

---

## Step 7

実画像による検証を行う。

可能であれば以下の画像を保存してテストできるようにする。

```text
QVGA画像
FullHD画像
黄色ボール画像
赤色壁画像
複数壁画像
ノイズを含む画像
```

---

# 22. 完了条件

以下をすべて満たした時点で実装完了とする。

* [ ] BGR numpy.ndarrayを入力として受け取れる
* [ ] 任意の画像解像度で動作する
* [ ] 黄色HSV範囲を外部設定できる
* [ ] 赤色HSV範囲を外部設定できる
* [ ] 黄色ボールの高速存在検出ができる
* [ ] 黄色ボールの中心座標を推定できる
* [ ] 黄色ボールの直径を推定できる
* [ ] RANSACを使用して外れ値に対応できる
* [ ] RANSACインライア率を信頼度として返す
* [ ] 複数の赤色壁エッジを検出できる
* [ ] 最も下側の赤色エッジを選択できる
* [ ] エッジを `y = ax + b` 形式で返せる
* [ ] 垂直線などの扱いが定義されている
* [ ] 単体テストがある
* [ ] 複数解像度のテストがある
* [ ] 人工的な円画像によるRANSACテストがある
* [ ] 複数の赤色エッジによる選択テストがある
* [ ] READMEまたはモジュールドキュメントにAPI仕様が記載されている


# 23. 重要なアルゴリズム上の制約:

1. ボールの円フィッティングでは、黄色マスクの全ピクセルを円周点として使用してはならない。
   黄色領域の輪郭点を抽出し、輪郭点をRANSAC円フィッティングの入力とすること。

2. ボール推定の信頼度は、RANSACのインライア率とすること。

3. 赤色壁エッジは、赤色領域の下端境界を検出すること。
   赤色領域の上端や、赤色領域内部の任意のエッジを検出するものではない。

4. 赤色領域の下端は、各x座標における赤色領域の最大y座標を基本として抽出すること。

5. 複数の赤色領域が存在する場合、各領域の下端エッジ候補を生成し、
   画像内で最も下側にある候補を選択すること。

6. 直線フィッティングには、局所的な色ノイズやマスク欠損に対応するため、
   RANSAC直線フィッティングを使用することを推奨する。

7. 最終的な赤色エッジは y = ax + b で表現する。